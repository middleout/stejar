{
  "name": "stejar",
  "description": "Web development utilities for React and Typescript",
  "private": true,
  "license": "MIT",
  "scripts": {
    "postinstall": "lerna bootstrap --hoist --concurrency=1",
    "bootstrap": "lerna bootstrap --hoist",
    "build": "lerna run test && lerna run build && cd __integration__ && npm i @stejar3/di && npm i @stejar3/react-di && npm i @stejar3/react && cd ../"
  },
  "devDependencies": {
    "lerna": "^2.0.0-rc.5"
  }
}
