{
  "name": "@stejar/application",
  "version": "1.0.10",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "dev": "rm -rf ./dist && tsc --outDir ./dist",
    "build": "rm -rf ./dist && tsc --outDir ./dist && npm version patch && npm publish --access=public"
  },
  "dependencies": {
    "@stejar/di": "^1.0.9",
    "@types/node": "6.0.49",
    "babel-polyfill": "6.20.0",
    "es6-promise": "4.0.5"
  },
  "devDependencies": {
    "ts-loader": "1.3.2",
    "tslib": "1.4.0",
    "typescript": "2.1.4"
  }
}
