{
  "name": "stejar",
  "version": "1.0.0",
  "description": "Web development utilities for React and Typescript",
  "private": true,
  "license": "MIT",
  "scripts": {
    "postinstall": "node scripts/build-package-json.js && node scripts/setup-symlinks.js && lerna bootstrap --concurrency=1 && npm run build && cd __integration && npm install && cd ../",
    "build": "lerna run test && lerna run build && node scripts/install-packages-into-integration.js",
    "update": "node scripts/update-dependencies.js",
    "clean": "lerna clean && node scripts/clean.js && rimraf ./node_modules"
  },
  "devDependencies": {
    "lerna": "^2.0.0-rc.5",
    "npm-check-updates": "^2.12.1",
    "rimraf": "^2.6.1"
  }
}
