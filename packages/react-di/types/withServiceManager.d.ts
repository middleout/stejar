/**
 * A public Higher Order Component to inject the Service Manager
 * inside a component via props
 *
 * @param WrappedComponent
 * @returns {any}
 */
export declare function withServiceManager(WrappedComponent: any): any;
