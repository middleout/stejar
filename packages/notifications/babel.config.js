/**
 * We want to have the "root dir" of babel here (in the package dir)
 * but want to use the actual config from the common scripts
 */
module.exports = require("./../../scripts/babel.react.preset");
