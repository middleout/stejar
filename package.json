{
  "name": "stejar",
  "description": "Web development utilities for React and Typescript",
  "private": true,
  "license": "MIT",
  "scripts": {
    "postinstall": "lerna bootstrap --concurrency=1",
    "build": "lerna run test && lerna run build && cd __integration && npm i @stejar3/di && npm i @stejar3/react && cd ../"
  },
  "devDependencies": {
    "lerna": "^2.0.0-rc.5"
  }
}
